const initialState = {
  meter: 3,
  bpm: 60,
  key: {
    level: 'c',
    majorMinor: 'major'
  }
}

const controllerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
